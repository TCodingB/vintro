<script setup lang="ts">import {MapPin,ArrowRightLeft} from 'lucide-vue-next';import PageHeader from '../components/PageHeader.vue';import {ownershipHistory} from '../data/customer';import {useSelectedVehicle} from '../composables/useSelectedVehicle';const {vehicle:v,base}=useSelectedVehicle()
</script>
<template><PageHeader title="Ownership history" :back="base"/><div class="container-app max-w-3xl py-7"><p class="eyebrow">Vehicle provenance</p><h1 class="page-title mt-2">Ownership history</h1><p class="mt-3 text-muted">The vehicle record stays with the car while private owner information stays protected.</p><div class="card mt-7 flex items-center gap-4 p-4"><img :src="v.image" class="h-16 w-24 rounded-xl object-cover"/><div><strong>{{v.make}} {{v.model}}</strong><p class="mt-1 text-sm text-muted">VIN ending {{v.vin.slice(-4)}}</p></div></div><div class="mt-8"><article v-for="(o,i) in ownershipHistory" class="relative pb-8 pl-9 last:pb-0 before:absolute before:left-[9px] before:top-5 before:h-full before:w-px before:bg-line last:before:hidden"><span class="absolute left-0 top-2 grid h-5 w-5 place-items-center rounded-full bg-accent ring-4 ring-canvas"><span class="h-1.5 w-1.5 rounded-full bg-white"/></span><div class="card p-5"><div class="flex items-start justify-between"><div><h2 class="font-semibold">{{o.owner}}</h2><p class="mt-1 text-sm text-muted">{{o.period}}</p></div><span v-if="o.current" class="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-success">Current</span></div><p class="mt-4 flex items-center gap-2 text-sm text-muted"><MapPin :size="15"/>{{o.location}}</p></div></article></div><RouterLink :to="`${base}/transfer`" class="btn-primary mt-8 w-full"><ArrowRightLeft :size="18"/>Transfer to a new owner</RouterLink><p class="mt-4 text-center text-xs leading-5 text-muted">Private documents and contact details never transfer to the next owner.</p></div></template>
