<script setup lang="ts">
import { useLocale } from '../locales';
const { translate } = useLocale();
import {ref} from 'vue';import {Shield,Upload,CalendarDays,Link2,CheckCircle2} from 'lucide-vue-next';import PageHeader from '../components/PageHeader.vue';import {useVintro} from '../composables/useVintro';const {notify}=useVintro();const connected=ref(false);const upload=()=>notify('Insurance policy added');const connect=()=>{connected.value=true;notify('Insurance provider connected in prototype')}
</script>
<template><PageHeader :title="translate('Insurance')" back="/profile"/><div class="container-app max-w-3xl py-7"><p class="eyebrow">{{ translate("Vehicle protection") }}</p><h1 class="page-title mt-2">{{ translate("Insurance policies") }}</h1><p class="mt-3 text-muted">{{ translate("Store policy documents and receive reminders before renewal.") }}</p><section class="card mt-7 p-5"><div class="flex items-start justify-between"><span class="grid h-11 w-11 place-items-center rounded-xl bg-emerald-50 text-success"><Shield/></span><span class="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-success">{{ translate("Active") }}</span></div><h2 class="mt-5 font-semibold">{{ translate("Comprehensive insurance") }}</h2><p class="mt-1 text-sm text-muted">{{ translate("BMW 320d Touring · Helvetia") }}</p><div class="mt-5 grid grid-cols-2 gap-4 text-sm"><span><small class="block text-muted">{{ translate("Policy number") }}</small><strong>{{ translate("CH-4829-2026") }}</strong></span><span><small class="block text-muted">{{ translate("Valid until") }}</small><strong>{{ translate("31 December 2026") }}</strong></span></div><p class="mt-4 flex items-center gap-2 rounded-xl bg-amber-50 p-3 text-sm text-amber"><CalendarDays :size="17"/>{{ translate("Renewal reminder enabled") }}</p></section><div class="mt-5 grid grid-cols-2 gap-3"><button @click="upload" class="btn-secondary"><Upload :size="17"/>{{ translate("Upload policy") }}</button><button @click="connect" class="btn-secondary"><CheckCircle2 v-if="connected" :size="17" class="text-success"/><Link2 v-else :size="17"/>{{ translate((connected?'Connected':'Connect provider')) }}</button></div><p class="mt-6 text-xs leading-5 text-muted">{{ translate("Provider integration is a prototype concept. No insurer is contacted and no policy data leaves this application.") }}</p></div></template>
