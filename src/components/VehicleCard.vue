<script setup lang="ts">
import { useLocale } from '../locales';
const { translate } = useLocale();
import {ArrowUpRight} from 'lucide-vue-next';import StatusBadge from './StatusBadge.vue';import type {Vehicle} from '../data/mock';defineProps<{vehicle:Vehicle;primary?:boolean}>()

</script>
<template><RouterLink :to="`/vehicle/${vehicle.id}`" class="card group block overflow-hidden hover:-translate-y-0.5 hover:shadow-soft"><div class="relative h-48 overflow-hidden bg-[#e9e5df]"><img :src="vehicle.image" :alt="translate(`${vehicle.make} ${vehicle.model}`)" class="h-full w-full object-cover transition duration-500 group-hover:scale-[1.02]"/><span v-if="primary" class="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1 text-xs font-semibold backdrop-blur">{{ translate("Primary vehicle") }}</span><span v-else class="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1 text-xs font-semibold backdrop-blur">{{ translate((vehicle.ownershipStatus) + " vehicle") }}</span><span class="absolute right-4 top-4 grid h-9 w-9 place-items-center rounded-full bg-white/90"><ArrowUpRight :size="17"/></span></div><div class="p-5"><div class="flex items-start justify-between gap-4"><div><p class="eyebrow">{{ translate((vehicle.trim)) }}</p><h2 class="mt-1 text-xl font-semibold tracking-tight">{{ translate((vehicle.make) + " " + (vehicle.model)) }}</h2><p class="mt-1 text-xs text-muted">{{ translate("Owned " + (vehicle.ownershipSince)) }}</p></div><StatusBadge :status="vehicle.health"/></div><div class="mt-5 flex items-end justify-between border-t pt-4"><div><p class="text-xs text-muted">{{ translate("Current mileage") }}</p><p class="nums mt-1 font-semibold">{{ translate((vehicle.mileage.toLocaleString('en-CH')) + " km") }}</p></div><div class="text-right"><p class="text-xs text-muted">{{ translate("Next") }}</p><p class="mt-1 text-sm font-medium">{{ translate((vehicle.id==='golf-gti'?'Inspection in 3,200 km':'Oil service in 2,580 km')) }}</p></div></div></div></RouterLink></template>
